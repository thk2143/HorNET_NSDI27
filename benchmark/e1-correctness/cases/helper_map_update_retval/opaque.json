{
  "_targets": "over every packet and map: the update can fail, so success is not provable and the failure path is live",
  "_expect": {"update_may_fail": "violated", "failure_reachable": "sat"},
  "exit_conditions": {
    "update_may_fail":   {"assert": "all", "cond": {"or": [{"ret": 0}, {"ret": 2}]}},
    "failure_reachable": {"assert": "exists", "cond": {"ret": 1}}
  }
}
